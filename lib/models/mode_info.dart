import 'package:flutter/material.dart';

class ModeInfo {
  final IconData icon;
  final String label;
  final Color color;
  const ModeInfo({
    required this.icon,
    required this.label,
    required this.color,
  });
}